<div class="chat-header">
 <div class="left-header">
   <div class="header-title">
    まさお
   </div>
  </div>
  <div class="right-header">
   <div class="header-button">
    <a href="#">チャットを終了する</a>
  </div>
  </div>
</div>

<div class="messages">
  <%= render partial: 'message', collection: @messages %>
</div>

<%= form_with model: [@room, @message], class: 'form', local: true do |f| %>
  <div class="form-input">
     <%= f.text_field :content, class: 'form-message', placeholder: 'type a message' %>
     <label class="form-image">
       <span class="image-file">画像</span>
       <%= f.file_field :image, class: 'hidden' %>
      </label>
    </div>
    <%= f.submit '送信', class: 'form-submit' %>
<% end %>


 <%# <div class="form"> %>
 <%# <div class="form-input">
    <input class="form-message" placeholder= "type a message">
    <label class="form-image">
     <span class="image-file">画像</span>
     <input type="file" class="hidden"> %>
     <%# ファイルを見えなくすると画像選択を使えなくなるのでlabel要素で押したように選択できる %>
    <%# </label> %>
    <%# label要素とはフォームの中でフォームの項目名とフォームの入力要素を関連付けるための要素
    役割の一つはlabel要素がクリックされたときに挟んだ子要素もクリックされたことにしてくれる
    この仕組を使うと見えないはずのファイル選択ボタンをクリックしたかのように動作させることができる %>
  <%# </div>
  <input class="form-submit" type="submit" value="送信"> %>
</div>
